import java.util.ArrayList;

class BFSGraphTreversal{
    LinkedList<Integer> obj = new LinkedList<>();
    BFSGraphTreversal(int element){
        boolean visited[] = new visited[v];
        if(!visited[s]){
            visited[s] = true;
            queue.add();
        }
        while(!i.hasNext()){
            int n = next();
            n.visited[i] = true;
            queue.add(n);
        }

    }









    public static void main(String[] args){
        ArrayList<ArrayList<Integer> > graph = new ArrayList<ArrayList<Integer>>();
        BFSGraphTreversal graph = new BFSGraphTreversal(5);
        graph.add(graph,2,3);
        graph.add(graph,4,2);

    }
}